# 订单统计卡片行

## 目标

在全部订单明细表和汇总表中，在筛选工具栏与表格之间展示一行统计卡片。

## 范围

- 新增可复用卡片/卡片行组件，支持通过 props 控制背景色与文字色。
- 明细表展示：照片总张数、接单总价、派单总价。
- 汇总表展示：订单总数、照片总张数、接单总价、派单总价、利润。
- 卡片默认尺寸 190px * 70px，带淡阴影。
- 卡片通过 v-for 渲染；一行放不下时不换行，自动收缩宽度和字体。

## 验收

- `npm run build` 通过。
- 两个表格组件均在表格上方显示统计卡片行。
- 窄容器下卡片保持一行显示，不出现换行。
